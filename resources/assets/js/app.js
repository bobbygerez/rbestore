
import Master from './layouts/google-contacts.vue'

import Vue from 'vue'
import Vuetify from 'vuetify'

Vue.use(Vuetify)

const app = new Vue({
    el: '#app',
    components: {
    	Master
    }
});
